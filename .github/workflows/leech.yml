name: Leech to Drive
run-name: Ferry transfer ${{ inputs.job_id }}

on:
  workflow_dispatch:
    inputs:
      source_url:
        description: Magnet link or HTTP/HTTPS URL
        required: true
        type: string
      job_id:
        description: Ferry job ID
        required: true
        type: string
      callback_url:
        description: Ferry webhook URL
        required: true
        type: string
      drive_folder:
        description: Google Drive folder
        required: false
        default: Ferry
        type: string

permissions:
  contents: read

jobs:
  leech:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Validate runner secrets
        env:
          RCLONE_CONF: ${{ secrets.RCLONE_CONF }}
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
        run: |
          test -n "$RCLONE_CONF" || (echo "RCLONE_CONF secret is missing." && exit 1)
          test -n "$WEBHOOK_SECRET" || (echo "WEBHOOK_SECRET secret is missing." && exit 1)

      - name: Report started
        env:
          CALLBACK_URL: ${{ inputs.callback_url }}
          JOB_ID: ${{ inputs.job_id }}
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
          WORKFLOW_RUN_ID: ${{ github.run_id }}
        run: |
          python3 - <<'PY'
          import hashlib, hmac, json, os, urllib.request
          body = json.dumps({
              "jobId": os.environ["JOB_ID"],
              "status": "running",
              "workflowRunId": os.environ["WORKFLOW_RUN_ID"],
          }, separators=(",", ":")).encode()
          sig = "sha256=" + hmac.new(os.environ["WEBHOOK_SECRET"].encode(), body, hashlib.sha256).hexdigest()
          req = urllib.request.Request(os.environ["CALLBACK_URL"], data=body, method="POST", headers={
              "Content-Type": "application/json",
              "X-Ferry-Signature": sig,
          })
          try:
              urllib.request.urlopen(req, timeout=10).read()
          except Exception as exc:
              print(f"Webhook start report failed: {exc}")
          PY

      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y aria2 jq
          curl -fsSL https://rclone.org/install.sh | sudo bash

      - name: Configure rclone
        env:
          RCLONE_CONF: ${{ secrets.RCLONE_CONF }}
        run: |
          mkdir -p ~/.config/rclone
          printf '%s' "$RCLONE_CONF" > ~/.config/rclone/rclone.conf
          rclone listremotes

      - name: Download source
        env:
          SOURCE_URL: ${{ inputs.source_url }}
          CALLBACK_URL: ${{ inputs.callback_url }}
          JOB_ID: ${{ inputs.job_id }}
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
          WORKFLOW_RUN_ID: ${{ github.run_id }}
        run: |
          mkdir -p out
          mkdir -p ~/.cache/aria2
          python3 - <<'PY'
          import hashlib, hmac, json, os, secrets, subprocess, time, urllib.parse, urllib.request

          callback_url = os.environ["CALLBACK_URL"]
          job_id = os.environ["JOB_ID"]
          webhook_secret = os.environ["WEBHOOK_SECRET"].encode()
          workflow_run_id = os.environ["WORKFLOW_RUN_ID"]
          rpc_secret = secrets.token_urlsafe(24)
          source_url = os.environ["SOURCE_URL"]
          extra_trackers = [
              "udp://tracker.opentrackr.org:1337/announce",
              "udp://open.stealth.si:80/announce",
              "udp://tracker.torrent.eu.org:451/announce",
              "udp://tracker.dler.org:6969/announce",
              "udp://open.demonii.com:1337/announce",
              "udp://exodus.desync.com:6969/announce",
              "udp://tracker-udp.gbitt.info:80/announce",
              "udp://opentracker.i2p.rocks:6969/announce",
              "https://tracker.tamersunion.org:443/announce",
              "https://tracker.gbitt.info:443/announce",
          ]

          def merged_trackers(value):
              if not value.startswith("magnet:?"):
                  return ",".join(extra_trackers)
              params = urllib.parse.parse_qs(urllib.parse.urlparse(value).query)
              existing = [item for values in params.get("tr", []) for item in values.splitlines()]
              trackers = []
              seen = set()
              for tracker in [*existing, *extra_trackers]:
                  if tracker and tracker not in seen:
                      trackers.append(tracker)
                      seen.add(tracker)
              return ",".join(trackers)

          def report(status, phase, downloaded=0, total=0, speed=0, error=None):
              body = {
                  "jobId": job_id,
                  "status": status,
                  "workflowRunId": workflow_run_id,
                  "progressPhase": phase,
                  "downloadedBytes": int(downloaded or 0),
                  "totalBytes": int(total or 0),
                  "speedBytesPerSecond": int(speed or 0),
              }
              if error:
                  body["errorMessage"] = error
              data = json.dumps(body, separators=(",", ":")).encode()
              sig = "sha256=" + hmac.new(webhook_secret, data, hashlib.sha256).hexdigest()
              req = urllib.request.Request(callback_url, data=data, method="POST", headers={
                  "Content-Type": "application/json",
                  "X-Ferry-Signature": sig,
              })
              try:
                  urllib.request.urlopen(req, timeout=10).read()
              except Exception as exc:
                  print(f"Progress webhook failed: {exc}")

          def rpc(method, params=None):
              payload = json.dumps({
                  "jsonrpc": "2.0",
                  "id": "ferry",
                  "method": method,
                  "params": [f"token:{rpc_secret}", *(params or [])],
              }).encode()
              req = urllib.request.Request("http://127.0.0.1:6800/jsonrpc", data=payload, method="POST", headers={
                  "Content-Type": "application/json",
              })
              with urllib.request.urlopen(req, timeout=10) as response:
                  result = json.loads(response.read().decode())
              if "error" in result:
                  raise RuntimeError(result["error"])
              return result.get("result")

          command = [
              "aria2c",
              "--dir=out",
              "--seed-time=0",
              "--bt-stop-timeout=1800",
              "--lowest-speed-limit=1K",
              "--timeout=60",
              "--connect-timeout=30",
              "--max-tries=5",
              "--retry-wait=20",
              "--max-connection-per-server=16",
              "--max-concurrent-downloads=1",
              "--split=16",
              "--min-split-size=1M",
              "--bt-enable-lpd=true",
              "--bt-save-metadata=true",
              "--bt-load-saved-metadata=true",
              "--bt-max-peers=200",
              "--bt-tracker-connect-timeout=30",
              "--bt-tracker-timeout=60",
              "--bt-request-peer-speed-limit=0",
              f"--bt-tracker={merged_trackers(source_url)}",
              "--enable-dht=true",
              "--enable-dht6=true",
              "--enable-peer-exchange=true",
              "--dht-listen-port=6881-6999",
              "--listen-port=6881-6999",
              "--follow-torrent=mem",
              "--file-allocation=falloc",
              "--disk-cache=128M",
              "--enable-mmap=true",
              "--max-overall-download-limit=0",
              "--max-download-limit=0",
              "--summary-interval=30",
              "--console-log-level=notice",
              "--log=aria2.log",
              "--log-level=notice",
              "--enable-rpc=true",
              "--rpc-listen-all=false",
              "--rpc-listen-port=6800",
              f"--rpc-secret={rpc_secret}",
              source_url,
          ]

          report("running", "downloading")
          process = subprocess.Popen(command)
          last_report = 0
          downloaded = total = speed = 0
          try:
              while process.poll() is None:
                  time.sleep(5)
                  try:
                      active = rpc("aria2.tellActive")
                      waiting = rpc("aria2.tellWaiting", [0, 10])
                      stopped = rpc("aria2.tellStopped", [0, 10])
                      items = list(active or []) + list(waiting or []) + list(stopped or [])
                      if items:
                          downloaded = sum(int(item.get("completedLength", 0)) for item in items)
                          total = sum(int(item.get("totalLength", 0)) for item in items)
                          speed = sum(int(item.get("downloadSpeed", 0)) for item in active or [])
                      now = time.time()
                      if now - last_report >= 15:
                          report("running", "downloading", downloaded, total, speed)
                          last_report = now
                  except Exception as exc:
                      print(f"Progress check failed: {exc}")

              status = process.wait()
              report("running", "uploading", downloaded, total, 0)
              if status != 0:
                  message = "Download failed or stalled. This usually means the torrent has no active seeders, no peers, or the source URL is unavailable."
                  print(message)
                  subprocess.run(["tail", "-n", "120", "aria2.log"], check=False)
                  raise SystemExit(status)
          finally:
              try:
                  rpc("aria2.shutdown")
              except Exception:
                  pass
          PY

      - name: Build upload manifest
        run: |
          find out -type f -printf '%P\n' | sort > upload-manifest.txt
          test -s upload-manifest.txt || (echo "Download produced no files." && exit 1)
          cat upload-manifest.txt

      - name: Upload to Drive
        env:
          DRIVE_FOLDER: ${{ inputs.drive_folder }}
        run: |
          rclone mkdir "gdrive:${DRIVE_FOLDER}"
          rclone move out "gdrive:${DRIVE_FOLDER}" \
            --progress \
            --transfers=8 \
            --checkers=16 \
            --drive-chunk-size=256M \
            --drive-upload-cutoff=256M \
            --fast-list \
            --delete-empty-src-dirs

      - name: Report success
        if: success()
        env:
          CALLBACK_URL: ${{ inputs.callback_url }}
          JOB_ID: ${{ inputs.job_id }}
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
          DRIVE_FOLDER: ${{ inputs.drive_folder }}
          WORKFLOW_RUN_ID: ${{ github.run_id }}
        run: |
          python3 - <<'PY'
          import hashlib, hmac, json, os, subprocess, urllib.parse, urllib.request

          drive_folder = os.environ["DRIVE_FOLDER"]
          with open("upload-manifest.txt", "r", encoding="utf-8") as manifest:
              first_file = next((line.strip() for line in manifest if line.strip()), "")

          fallback = f"https://drive.google.com/drive/search?q={urllib.parse.quote(drive_folder)}"
          drive_link = fallback
          if first_file:
              remote_path = f"gdrive:{drive_folder}/{first_file}"
              try:
                  link = subprocess.check_output(
                      ["rclone", "link", remote_path],
                      text=True,
                      stderr=subprocess.STDOUT,
                      timeout=30,
                  ).strip()
                  if link:
                      drive_link = link
              except Exception as exc:
                  print(f"Unable to create direct Drive link for {remote_path}: {exc}")

          body = json.dumps({
              "jobId": os.environ["JOB_ID"],
              "status": "completed",
              "driveLink": drive_link,
              "workflowRunId": os.environ["WORKFLOW_RUN_ID"],
          }, separators=(",", ":")).encode()
          sig = "sha256=" + hmac.new(os.environ["WEBHOOK_SECRET"].encode(), body, hashlib.sha256).hexdigest()
          req = urllib.request.Request(os.environ["CALLBACK_URL"], data=body, method="POST", headers={
              "Content-Type": "application/json",
              "X-Ferry-Signature": sig,
          })
          urllib.request.urlopen(req, timeout=20).read()
          PY

      - name: Report failure
        if: failure()
        env:
          CALLBACK_URL: ${{ inputs.callback_url }}
          JOB_ID: ${{ inputs.job_id }}
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
          WORKFLOW_RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          python3 - <<'PY'
          import hashlib, hmac, json, os, urllib.request
          run_url = f"https://github.com/{os.environ['GITHUB_REPOSITORY']}/actions/runs/{os.environ['WORKFLOW_RUN_ID']}"
          body = json.dumps({
              "jobId": os.environ["JOB_ID"],
              "status": "failed",
              "workflowRunId": os.environ["WORKFLOW_RUN_ID"],
              "errorMessage": f"GitHub Actions runner failed. Check workflow logs: {run_url}",
          }, separators=(",", ":")).encode()
          sig = "sha256=" + hmac.new(os.environ["WEBHOOK_SECRET"].encode(), body, hashlib.sha256).hexdigest()
          req = urllib.request.Request(os.environ["CALLBACK_URL"], data=body, method="POST", headers={
              "Content-Type": "application/json",
              "X-Ferry-Signature": sig,
          })
          try:
              urllib.request.urlopen(req, timeout=20).read()
          except Exception as exc:
              print(f"Webhook failure report failed: {exc}")
          PY
